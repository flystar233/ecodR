% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{get_outliers}
\alias{get_outliers}
\title{Identify Outliers Based on Threshold}
\usage{
get_outliers(object, threshold = "auto", return_indices = FALSE)
}
\arguments{
\item{object}{An ecod object.}

\item{threshold}{Numeric threshold or character string. If numeric, samples with
scores above this value are considered outliers. If character, one of:
  \itemize{
    \item "auto" - Uses 95th percentile (default)
    \item A percentile like "0.95", "0.99"
  }}

\item{return_indices}{Logical. If TRUE, returns indices of outliers.
If FALSE (default), returns a logical vector.}
}
\value{
Either a logical vector (if return_indices=FALSE) or integer vector
of outlier indices (if return_indices=TRUE).
}
\description{
Returns indices or logical vector indicating which samples are outliers.
}
\examples{
model <- ecod(iris[, 1:4])

# Get logical vector
is_outlier <- get_outliers(model)
table(is_outlier)

# Get indices
outlier_indices <- get_outliers(model, return_indices = TRUE)
print(outlier_indices)

# Use custom threshold
outliers <- get_outliers(model, threshold = 0.99)

}

